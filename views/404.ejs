<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - 系统边界</title>
    <style>
        :root {
            --bg-color: #080808;
            --text-color: #f0f0f0;
            --accent-color: #00fffc; /* 青色 */
            --error-color: #fc00ff;  /* 洋红 */
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Courier New', Courier, monospace; /* 等宽字体增强代码感 */
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        /* 背景网格线，增加空间感 */
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%),
                linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            background-size: 100% 2px, 3px 100%;
            pointer-events: none;
            z-index: 1;
        }

        .container {
            position: relative;
            z-index: 2;
            text-align: center;
        }

        /* 巨大的故障文字 */
        .glitch {
            font-size: 8rem;
            font-weight: bold;
            text-transform: uppercase;
            position: relative;
            text-shadow: 0.05em 0 0 var(--error-color), -0.025em -0.05em 0 var(--accent-color),
                        0.025em 0.05em 0 var(--text-color);
            animation: glitch 500ms infinite;
        }

        .glitch span {
            position: absolute;
            top: 0;
            left: 0;
        }

        .subtitle {
            font-size: 1.5rem;
            margin-top: 1rem;
            letter-spacing: 0.5em;
            opacity: 0.8;
            margin-bottom: 3rem;
        }

        /* 帅气的按钮 */
        .btn {
            text-decoration: none;
            color: var(--bg-color);
            background: var(--accent-color);
            padding: 1rem 3rem;
            font-size: 1.2rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            transition: all 0.3s ease;
            clip-path: polygon(10% 0, 100% 0, 100% 70%, 90% 100%, 0 100%, 0 30%);
        }

        .btn:hover {
            background: var(--error-color);
            color: white;
            box-shadow: 0 0 20px var(--error-color);
        }

        /* 故障动画关键帧 - 模拟信号撕裂 */
        @keyframes glitch {
            0% {
                text-shadow: 0.05em 0 0 var(--error-color), -0.05em -0.025em 0 var(--accent-color),
                            -0.025em 0.05em 0 var(--text-color);
            }
            14% {
                text-shadow: 0.05em 0 0 var(--error-color), -0.05em -0.025em 0 var(--accent-color),
                            -0.025em 0.05em 0 var(--text-color);
            }
            15% {
                text-shadow: -0.05em -0.025em 0 var(--error-color), 0.025em 0.025em 0 var(--accent-color),
                            -0.05em -0.05em 0 var(--text-color);
            }
            49% {
                text-shadow: -0.05em -0.025em 0 var(--error-color), 0.025em 0.025em 0 var(--accent-color),
                            -0.05em -0.05em 0 var(--text-color);
            }
            50% {
                text-shadow: 0.025em 0.05em 0 var(--error-color), 0.05em 0 0 var(--accent-color),
                            0 -0.05em 0 var(--text-color);
            }
            99% {
                text-shadow: 0.025em 0.05em 0 var(--error-color), 0.05em 0 0 var(--accent-color),
                            0 -0.05em 0 var(--text-color);
            }
            100% {
                text-shadow: -0.025em 0 0 var(--error-color), -0.025em -0.025em 0 var(--accent-color),
                            -0.025em -0.05em 0 var(--text-color);
            }
        }

        /* 底部的小字 */
        .footer {
            position: absolute;
            bottom: 2rem;
            font-size: 0.8rem;
            color: #444;
        }

    </style>
</head>
<body>

    <div class="container">
        <div class="glitch" data-text="404">404</div>
        <div class="subtitle">DATA NOT FOUND</div>
        
        <a href="/" class="btn">Return Home</a>
    </div>

    <div class="footer">
        SYSTEM_ERR_CODE: CLIENT_SIDE_FAILURE
    </div>

</body>
</html>